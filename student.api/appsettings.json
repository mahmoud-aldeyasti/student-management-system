{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "studentCon": "Host=aws-1-eu-west-1.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.swaszgxsicqwrcjlnyjh;Password=StudentManagementDatabase;SSL Mode=Require;"
    // note that what you type here's is for your local machine render will still use the environment variable for the connection string, so you can use this for local testing but it won't affect the render deployment
  },
  "AllowedHosts": "*"
}
